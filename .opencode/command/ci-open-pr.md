---
description: Push the current branch and open a PR with the locked enkinex template (explicit request only)
agent: pr-open
subtask: true
---

$ARGUMENTS

Follow the pr-open rules: verify the enkinex remote and branch slug, require the plan reference,
push with upstream tracking, and open the PR with the locked body template.
